csv_dir: csv
templates_dir: templates
base_url: https://bennuttall.com

build:
  posts_dir: content/posts
  pages_dir: content/pages
  static_dir: static
  blog_root: blog
  output_dir: www

logs:
  logs_dir: apache2
  pattern: "bennuttall.com-access*"

analytics:
  output_dir: analytics
  manifest_path: www/manifest.json
