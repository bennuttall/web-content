<div metal:use-macro="layout">
  <div metal:fill-slot="head" tal:omit-tag="1">
    <meta name="description" content="${page.description}" />
    <meta property="og:url" content="https://bennuttall.com/${page.slug}/" />
    <meta property="og:title" content="${page.title}" />
    <meta property="og:description" content="${page.description}" />
    <meta name="twitter:title" content="${page.title}" />
    <meta name="twitter:description" content="${page.description}" />
  </div>

  <div metal:fill-slot="title" tal:omit-tag="1">${page.html_title}</div>

  <section metal:fill-slot="content">
    <div>
      <div class="row">
        <div class="small-12 columns">
          <h1>${page.title}</h1>
        </div>
      </div>
      <div class="row">
        <div class="small-12 ${'' if page.full_width else 'medium-8'} columns entry-content">
          <div tal:content="structure page.html"></div>
        </div>
      </div>
    </div>
  </section>
</div>